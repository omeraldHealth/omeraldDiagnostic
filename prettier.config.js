module.exports = {
    trailingComma: 'all',     // Enforces trailing commas in all possible places
    singleQuote: true,        // Enforces single quotes
    semi: true,               // Ensures semicolons are used at the end of statements
    printWidth: 80,           // Sets the maximum line length to 80 characters
    tabWidth: 2,              // Specifies that each tab should be 2 spaces wide
    arrowParens: 'always'     // Enforces parentheses around arrow function arguments
  };
  